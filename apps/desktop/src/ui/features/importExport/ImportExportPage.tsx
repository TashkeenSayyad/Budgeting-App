export function ImportExportPage() {
  return (
    <div className="space-y-2">
      <h2 className="text-xl">Import / Export CSV</h2>
      <p className="text-slate-300">Wizard flow: file → mapping → preview → dedup → rules toggle → summary.</p>
      <p className="text-slate-300">Use Settings for database backup/restore.</p>
    </div>
  );
}
